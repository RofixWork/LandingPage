import styled from "styled-components"

import style from 'styled-components';
export const Feat = style.section`
    padding: 80px 0;
    background-color: #f5f5ff;
    .feat-content {
        .icon {
            color: crimson;
            font-size: 30px;
        }
        .feat-desc {
            font-size: 17px;
        }
    }
` 